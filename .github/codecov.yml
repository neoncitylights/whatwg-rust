# https://docs.codecov.com/docs/codecov-yaml
# https://docs.codecov.com/docs/codecovyml-reference
coverage:
  range: 90...100
  status:
    project:
      default:
        threshold: 2%
        if_ci_failed: error
